library create_challenge_ios_app.globals;

import 'package:flutter/material.dart';

Color backgroundColor = Colors.white;
Color ribbonColor = Color(0xffe9c9aa); //app bar at the top 
Color textColor = Colors.black; 
Color panicButtonColor = Colors.red; // color of panic button on home page

Color messageSettingsIconColor = Colors.lightGreen; //top left
Color infoCardIconColor = Colors.blue; // top right
Color historyIconColor = Colors.orange; // bottom left
Color dataIconColor = Colors.amber; // bottom right