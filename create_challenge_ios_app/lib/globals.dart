library create_challenge_ios_app.globals;

import 'package:flutter/material.dart';

const Color backgroundColor = Colors.white;
const Color ribbonColor = Color(0xffe9c9aa); //app bar at the top 
const Color textColor = Colors.black; 
const Color panicButtonColor = Colors.red; // color of panic button on home page

const Color messageSettingsIconColor = Colors.lightGreen; //top left
const Color infoCardIconColor = Colors.blue; // top right
const Color historyIconColor = Colors.orange; // bottom left
const Color dataIconColor = Colors.amber; // bottom right